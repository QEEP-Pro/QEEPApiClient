language: php

php:
  - 7.1
  - 7.2

before_script:
  - composer install
  - composer require indigophp/doctrine-annotation-autoload

stages:
  - lint
  - unit test

jobs:
  include:
    - stage: lint
      script: composer lint
    - stage: test
      script: composer test